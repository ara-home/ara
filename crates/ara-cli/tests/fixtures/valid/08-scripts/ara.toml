[project]
name = "scripts-app"
version = "0.1.0"

[scripts]
build = "tsc --strict"
test = "vitest run --reporter verbose"
lint = "eslint src/ --fix"
start = "node dist/index.js"
"pre:build" = "rm -rf dist"
