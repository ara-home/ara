#!/bin/sh
set -eu

echo "  HOOK   pre-commit"
cd "$(git rev-parse --show-toplevel)"

echo "  TEST   unit"
zig build test 2>/dev/null || {
    echo "  FAIL   unit tests failed, commit blocked"
    exit 1
}

echo "  PASS   all checks"
